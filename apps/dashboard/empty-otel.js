// Stub for @opentelemetry/api — satisfies Turbopack dev alias only.
// The webpack server build leaves the real package untouched so the
// bundled noop fallback inside @supabase/supabase-js can execute correctly.
module.exports = {};
